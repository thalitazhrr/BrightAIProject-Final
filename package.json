{
  "name": "telkom-hsi-brightai",
  "version": "1.0.0",
  "description": "Telkom HSI BrightAI - Complete Analytics & Intelligence Platform",
  "main": "start-dev.js",
  "scripts": {
    "start": "npm-run-all -p backend frontend",
    "backend": "npm --prefix backend start",
    "frontend": "npm --prefix frontend start",
    "install-all": "npm --prefix backend install && npm --prefix frontend install",
    "dev": "concurrently -n BACKEND,FRONTEND -c cyan,magenta --prefix time \"npm --prefix backend run dev\" \"npm --prefix frontend start\"",
    "build": "npm --prefix frontend run build",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "keywords": [
    "telkom",
    "hsi",
    "analytics",
    "ai",
    "dashboard",
    "brightai"
  ],
  "author": "Telkom HSI Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "npm-run-all": "^4.1.5"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}